# BeadMatch 游戏广场

> 版本：**v0.1** ｜ 日期：2026-09-15
> 状态：方向已定；**第 1 步（搬到 `games/beadmatch/` + 真包化）已做完**，第 2 步（抽广场外壳）还没开始
>
> 本文管「广场」这一层：项目怎么从「一个串珠游戏」长成「多个游戏的广场」，
> 文件怎么组织，每个游戏要对广场交代什么。
>
> 串珠游戏**本身**的规则、参数、数据格式、接口、界面仍在 [requirement.md](requirement.md)。
> 怎么跑起来见 [how_to_start.md](how_to_start.md)，改动的来龙去脉见 [discussion-2026-09.md](discussion-2026-09.md)。

---

## 1. 要做什么

把 BeadMatch 做成一个**游戏广场**：一个程序里放多个给小孩玩的游戏，
按认知能力分类（`plan` 规划 / `logic` 推理 / `memory` 记忆 / `pattern` 找规律 / `calc` 计算，
每类的核心能力和例子见 [../games/README.md](../games/README.md)）。

**串珠是其中第一个游戏**，属于「规划」类。它现在的东西（出题算法、题库、后端、前端、打包）
全部保留，只是位置和身份变了：从「整个项目」变成「广场里的一个游戏」。

---

## 2. 已定决策（2026-09-15）

| # | 问题 | 结论 | 连带影响 |
| --- | --- | --- | --- |
| 1 | 打包 | **保持现状，不重做**。打包是临时方案，只要求"搬家后还能打出现在的程序" | 搬完那次要改 `make_exe.bat` 的 3 处参数 + `launcher.py` 的 2 个默认路径（见 §7.1） |
| 2 | 题库放哪 | **每个游戏自己带**：`games/<game_id>/puzzles/` | 目录名仍叫 `puzzles/`，代码里的 `PUZZLE_DIR`、`BEADMATCH_PUZZLES` 都不改名，只改默认路径 |
| 3 | 进度 / 存档 | **不做**（不记玩过哪些题、不记成绩） | 广场层不需要任何存储模块，保持很薄。现状零改动——前端目前连 `localStorage` 都没用 |
| 4 | 项目总名 | **不改**，仓库 / exe / 图标继续叫 BeadMatch，内部用 `plaza/` 表示广场外壳 | 将来真要改名，一次性处理四处：GitHub remote、exe 名与图标、`make_exe.bat`、`%LOCALAPPDATA%\BeadMatch` |
| 5 | 跨游戏**格式**统一 | **不做元信息格式**，改为统一**骨架**（目录形状 + 代码接口） | 现有 300 个题目文件**一个字都不改** |

### 2.1 第 5 条为什么这么定

原本的设想是抽一个「元信息信封」（`game=` / `id=` / `level=` 等等），让所有游戏共用一套题目文件格式。
改成统一骨架，理由三条：

1. **只有一个游戏时，设计不出跨游戏的格式。** 记忆类、分类类游戏的题目长什么样，现在没人知道；
   凭空定的字段十有八九用不上。等第二个游戏真做出来，从两套**实际**格式里抽共同点，比现在猜准得多。
2. **广场真正依赖的不是文件格式，而是代码接口。** 广场要做的事只有：
   列出有哪些游戏、每个游戏有哪些等级、随机抽一道、按 id 取一道。
   这四件事调用游戏自己的函数就够了，广场**永远不直接读题目文件**。
3. **不用迁移数据。** 统一格式意味着 300 个文件都要插字段、重新提交；统一骨架一行数据都不用动。

代价：串珠这套 `created_at / created_by / solution_by / id / level / ...` 格式从
「项目标准」降级为**「串珠的私有格式」**。以后想做交换协议，再从多个游戏的实际格式里抽。

---

## 3. 目标目录结构

```
BeadMatch/
├─ run.py                     唯一入口：开发直接跑，打包也用它
├─ start/                     怎么跑、怎么发：打包脚本 / 桌面启动器 / 图标
├─ plaza/                     广场层（跟具体游戏无关）
│  ├─ server.py               FastAPI 主体：扫游戏、挂路由、托管静态
│  ├─ registry.py             扫 games/*/game.toml → 游戏清单
│  └─ web/                    广场首页 + 公共前端（语音 / 音效 / 键盘 / 主题）
├─ games/
│  ├─ beadmatch/              串珠（规划）：core + api + web + puzzles + tools + tests + game.toml
│  ├─ beadsort/               竞技串珠（规划）：只能摞同色/空柱（代码从 beadmatch 复制后改）
│  ├─ logic/                  推理：core + api + web + puzzles + game.toml（题是人写的）
│  ├─ memory/                 记忆：规矩不一样 —— 程序只给规则、不存答案
│  ├─ calc/                   计算：规则卡带答案，所以有 /check 能自动判
│  └─ pattern/                找规律（占位 README，还没做）
└─ docs/
```

一句话概括分层：

> **`plaza/` 管「有哪些游戏、怎么开、声音怎么响、界面长什么样」；
> `games/<game_id>/` 管「这个游戏的规则、出题、题库、界面」。**

---

## 4. 游戏对广场的契约

广场只依赖下面四样，别的一概不管（尤其是**不管**题目文件里写了什么）。

### 4.1 `game.toml`

广场扫描 `games/*/game.toml` 得到游戏清单，用于首页展示。

```toml
id = "beadmatch"
name = "串珠"
category = "plan"        # plan / logic / memory / pattern / calc
summary = "把珠子按颜色归位"
```

### 4.2 `core/` 必须暴露的三个函数

广场通过它们拿题，不碰文件：

| 函数 | 作用 | 返回 |
| --- | --- | --- |
| `list_levels()` | 每个等级各有多少道题 | `{level: count}` |
| `pick(level)` | 从某等级随机抽一道 | 一条题目 |
| `load(id)` | 按 id 取一道 | 一条题目 |

**「一条题目」的统一形状**（只有这三样是广场关心的，`body` 里是什么由游戏自己定）：

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一标识 |
| `level` | 难度等级（整数） |
| `body` | 游戏自己的数据，广场原样透传给前端 |

### 4.3 `api.py`

导出 `router`（FastAPI），由广场挂到 `/api/games/<game_id>/` 下。
广场自己只提供通用接口：

| 接口 | 说明 |
| --- | --- |
| `GET /api/games` | 有哪些游戏（读 `game.toml`） |
| `GET /api/games/{game}/levels` | 该游戏每个等级各多少题（调 `list_levels`） |
| `GET /api/games/{game}/random?level=N` | 随机抽一道（调 `pick`） |
| `GET /api/games/{game}/puzzles/{id}` | 取一道（调 `load`） |

### 4.4 `web/`

游戏的页面与交互。广场给它一个容器，它自己渲染；公共能力（语音、音效、键盘、主题）
由广场提供，游戏只需要说明「这道题该怎么念」。

---

## 5. 分层与边界

| 层 | 负责 | 不负责 |
| --- | --- | --- |
| 广场层 `plaza/` | 发现游戏、列出题目、随机抽题、托管静态页面、公共前端能力、打包外壳 | 任何游戏规则、任何题目文件内容 |
| 游戏层 `games/<id>/` | 规则、出题算法、题库文件、校验、本游戏界面 | 端口、静态托管、打包方式 |

**校验也跟着分两层**（现在混在 `core/generator.py` / `api.py` / 测试里，第 2 步抽广场外壳时切开）：

| 层 | 校验什么 |
| --- | --- |
| 广场层 | 只有信封性质的东西：`id` 与文件名一致、`level` 能解析成整数 |
| 游戏层（串珠） | 行列数、空位必须在顶端、颜色集合、每色颗数、`level == ceil(moves / 10)`、`solution` 步数对得上且套回去真能解开 |

`level == ceil(moves / 10)` 属于串珠的私有规则（它是"乱走多少步"推导出来的），**不要提到广场层**。

---

## 6. 这一版明确不做

| 不做 | 原因 |
| --- | --- |
| 进度 / 存档 / 数据库 | 决策 3。广场因此不需要存储层 |
| 插件系统、动态加载、热插拔 | 几个游戏而已，`games/` 下加个文件夹 + 一行注册就够 |
| 改题目文件格式（加字段、改 JSON、改扩展名） | 决策 5。300 个文件一个字不动 |
| 现在就定「跨游戏交换协议」 | 等第二个游戏做出来，从实际格式里抽（`§2.1` 第 1 条） |
| 重做打包 | 决策 1。只要"还能打出现在的程序" |
| 前端上框架 / 上构建工具 | 现在是原生 JS 三件套，离线可跑；广场首页也不需要框架 |

---

## 7. 迁移步骤

**原则：一次只做一件事，每步做完都能跑、能测、能提交。**

### 7.1 第 1 步：纯搬家 + 真包化 ✅ 已完成（2026-09-15）

只搬文件、改导入，**逻辑一行不动**。

- 串珠相关的东西收进 `games/beadmatch/`（含 `puzzles/` 的 300 个文件）。
- **真包化**：现在全仓库有 10 处 `sys.path.insert(...)` + `import free_solver` 这种顶层模块导入
  （`generator.py`、`walk_gen.py`、`server/app.py`、4 个测试文件、`make_puzzles.py`、`make_exe.bat`）。
  一个游戏时只是丑；两个游戏时同名顶层模块会互相覆盖，且报错难查。
  已改成真包导入（`from games.beadmatch.core.free_solver import ...`），各层加了 `__init__.py`。
- 打包跟着改了这些地方（否则"还能打出现在的程序"不成立）：

  | 文件 | 改前 | 改后 |
  | --- | --- | --- |
  | `make_exe.bat` | `--add-data "web;web"` | `--add-data "games\beadmatch\web;games/beadmatch/web"` |
  | `make_exe.bat` | `--add-data "puzzles;puzzles"` | `--add-data "games\beadmatch\puzzles;games/beadmatch/puzzles"` |
  | `make_exe.bat` | `--paths "solver"` + `--hidden-import free_solver` | **两行都删掉**（真包导入后 PyInstaller 自己找得到） |
  | `launcher.py` | `import server.app as app_module` | `import games.beadmatch.api as app_module` |
  | `launcher.py` | 数据目录 `<exe旁>/puzzles` | `<exe旁>/games/beadmatch/puzzles`（`BEADMATCH_WEB` 同理指向资源里的 `games/beadmatch/web`） |

**验收结果**：58 项测试全过；`make_exe.bat` 打出的 exe 起得来（`/api/health`、`/api/levels` 报 300、
首页和 `/static/app.js` 都是 200，题库自动拷到 exe 旁边的 `games\beadmatch\puzzles`）。

**入口变了**：`python -m server.app` → **`python run.py`**（`--host` / `--port` / `--reload` 三个参数照旧）；
打包仍是 `make_exe.bat`。命令都写进 [how_to_start.md](how_to_start.md) 了。

### 7.2 第 2 步：抽广场外壳 ✅ 已完成（2026-09-16）

`plaza/registry.py`（扫 `games/*/game.toml`）+ `plaza/server.py`（挂路由、托管首页和静态）
+ 广场首页。四个游戏各自加了自己的 `game.toml`；`api.py` 从"自己起一个 FastAPI app"
改成"导出一个 `router`"（单独跑照旧，端口不变）；`run.py` 改成起广场。

**这一版的 URL**（细节见 [how_to_start.md](how_to_start.md)）：

| 层 | 地址 |
| --- | --- |
| 广场首页 | `/` |
| 某个游戏 | `/games/<游戏>/` |
| 那个游戏的静态资源 | `/games/<游戏>/static/...` |
| 那个游戏的接口 | `/api/games/<游戏>/...` |
| 广场自己的接口 | `/api/games`、`/api/health` |

两条小约定：**广场只占 `/`、`/static/`、`/api/games`、`/api/health`、`/history`、`/api/history`**
（`/history` 是页脚的访问清单：最近 200 条"时间 / 游戏 / id"，数据见 `plaza/history.py`），其余空间全归各游戏；

访问清单的数据由各游戏自己写：`/random` 和 `/puzzles/{id}` 各调一次
`history.record_served(...)`，只记 `game / puzzle_id / level / 时间`，不碰题目内容。
跑测试时设 `BEADMATCH_HISTORY=off`，就不会往真实库里写。
游戏页面里的静态资源写成**相对路径**、接口基址读 `window.API_BASE`（广场注入），
所以同一份前端代码在"单独跑"和"广场里跑"两种情况下都对。

广场还往每个游戏页面里插一条**"换个游戏"小条**（`游戏广场 · 串珠 · 计算 · …`），
位置在**游戏标题上面**（先导航、后标题）：

- 字小、颜色淡、用游戏自己的主题变量（`--ink` / `--muted`），所以看着像游戏自带的；
- **能点的都带下划线**（鼠标移上去变深色），**"你正在看的这个"是加粗、没有下划线** ——
  一眼分得开"哪个是我"和"哪些能点"；
- 样式写在 `<head>` 里（跟接口基址一起注入），不会先闪一个没样式的导航。

**游戏侧一行代码都不用改**；单独跑某个游戏时也不会冒出别家的外链。

顺带定了一条小规矩：**游戏页面第一次加载时不发声**（点链接跳过来不该"嗒"一声），
声音只在人主动操作时响（按「换一题」/空格、点珠子、对答案……）。四个游戏的
`newPuzzle` / `newCard` 都加了 `{ silent: true }` 这一个入口。

还没做：公共前端（语音 / 音效 / 键盘 / 主题）仍在各游戏自己的 `web/` 里，等真要提再提；
打包（`start/launcher.py`）现在读的是 `plaza.server:app`，但要打成"四合一 exe"，
`make_exe.bat` 的 `--add-data` 还得补另外三个游戏的 `web/` 和 `puzzles/`。

**验收**：首页能看到四个游戏并点进去；每个游戏的行为跟搬家前完全一样。

### 7.3 第 3 步：写第二个游戏

**这一步才是真正的验收**：只有一个游戏时设计出来的抽象，十有八九是错的。
建议挑跟串珠差异最大的那一类（比如记忆类：看/听一段序列再复述），
因为它对「题怎么存、界面怎么渲染、声音怎么用」的假设跟串珠完全不同
—— 只有这样才试得出 `game.toml` 和 `plaza/` 的接口够不够用。

---

## 8. 待定

| # | 问题 | 备注 |
| --- | --- | --- |
| 1 | ~~广场首页要不要显示「每个等级各多少题」~~ | **已定：显示**（首页每张卡片上就写着"共 N 题（1 级 10 题、…）"），`levels()` 这条契约留着 |
| 2 | ~~第二个游戏做哪个（哪一类、什么玩法）~~ | **已做**：logic（推理）、memory（记忆）、calc（计算）三个都进广场了 |
| 3 | ~~`README.md` 和 `requirement.md` §9 里的过期信息~~ | **已清**（2026-09-15，跟第 1 步一起）：README 的「409 道题 / index.csv」、`requirement.md` §9 已定的条目都改了 |

---

## 9. 变更记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v0.1 | 2026-09-15 | 建立本文档：定下「游戏广场」方向 + 五条决策 + 目标目录结构 + 游戏契约 + 三步迁移路线（**尚未动代码**） |
| v0.1.1 | 2026-09-15 | **第 1 步做完**：串珠搬进 `games/beadmatch/`、真包化、入口改成 `run.py`、打包参数跟着改；测试 58 项全过 + exe 冒烟通过 |
| v0.1.2 | 2026-09-15 | **收拾根目录**：打包/启动相关的 5 个文件收进根目录的 `start/`（第 2 步不再建 `plaza/packaging/`，`plaza/` 只管代码）；**删除 `vendor/`**（旧求解器的 Pascal 原版存档）；`games/` 下按 6 大认知分类建了占位 README（**临时**，真做游戏时换成 `<游戏英文名>/` + `game.toml` 里的 `category`） |
| v0.1.3 | 2026-09-15 | 分类占位文件夹改成**英文名**：`group` / `sort` / `space` / `plan` / `logic` / `memory`，里边的 README 也改成**简单英文**（一两行说明这类游戏是什么）；`game.toml` 的 `category` 跟着用英文 id |
| v0.1.4 | 2026-09-15 | 打包收尾：PyInstaller 自动生成的 `BeadMatch.spec` 改放到 `build\`（根目录不再冒出来），为此把 `--add-data` / `--icon` / `--paths` 都改成绝对路径（`--specpath` 会让相对路径改成相对 spec 目录）；打包脚本仍是双击即用 |
| v0.1.5 | 2026-09-15 | 分类文件夹仍是英文名，但里边的 README 改成**中英双语**（中文写"核心能力 + 例子"，英文保留原来那两句简单句）；`games/README.md` 加了**六大类对照表**（文件夹 / 分类 / 核心能力 / 例子） |
| v0.2.0 | 2026-09-15 | **第二个游戏 `logic` 开工**（顺序推理）：`core/clues.py`（线索记号 + 求解）、`core/puzzles.py`（题目文件 + 校验 + 检查命令）、`puzzles/1/` 3 道题、35 项测试。用统一骨架（目录形状、文件格式的路子、扫题库接口都照抄串珠），**没有出题器** —— 题是人给的。顺带查出第 3 题原本有 4 个答案（补了 `O>Y`） |
| v0.2.1 | 2026-09-15 | logic 定稿题目格式：**正文放题目原句（一字不改）**，`participants` / `answer` 用颜色首字母（也能写中文或 emoji）；`beads` / `clues` 两个冗余字段删掉；新增 `core/chinese.py`（中文原句 → 记号，如 `红色在最前面。` → `R@1`），三种题都改成新格式。另加 `api.py`（`/api/logic/...`，能独立跑在 8010）。这版把"统一骨架"的第一次真实检验跑通了：目录形状、`core/ + api.py + puzzles/ + README` 都照串珠来 |
| v0.2.2 | 2026-09-15 | logic 加前端（`web/` 三件套，照串珠那套写：同一套主题/朗读/音效/键盘手感）。玩法是**点珠子放进槽里摆顺序、摆满自动检查、错的线索标红**；`api.py` 顺手托管静态页（打开 8010 根路径就是页面）。第二个游戏现在的形状：`core/ + api.py + web/ + puzzles/ + README` —— **和串珠完全同构**，广场外壳（第 2 步）可以直接照这个挂 |
| v0.2.3 | 2026-09-15 | 题库换血 + 格式微调：① 题目文件加**可选字段 `title`**（题目的名字，如"彩虹排队"）—— 有就显示、没有就空着，`api.py` 和前端都跟着改了；② `core/chinese.py` 从"按固定句型匹配"改成**抓关键信息**（第一颗提到的珠子是主语 → 看「第几个/最后/最前」→ 再看「在谁前/后」），所以"排在""坐在""睡在""跟在""第一个出发""拿到了第一支"这些说法都读得懂了；③ 题库换成 **10 道 1 级新题**（都带名字，全部验过 `解=1`），原来的 3 道删掉 |
| v0.2.4 | 2026-09-15 | 题库到 **30 道**（1/2/3 级各 10 道）；线索记号加两种：`X~~Y`（中间隔一颗）、`Y..X..Z`（X 夹在 Y 和 Z 中间），中文那边跟着认「中间隔一个」「在…和…中间」「挨在一起」。**放宽校验**：`answer` 跟线索对不上不再让整道题读不进来（题目的真相是线索，游戏判定也走线索），只在检查命令里提示一句 —— 这样数据源偶尔不严谨也不至于让某个等级点不开 |
| v0.2.5 | 2026-09-15 | logic 到 **L6、共 60 道**（1~6 级各 10 道）：L4/L5 的多解都补成唯一解（L5 按"距离+方向"重写，10 道全部唯一）。**L6 引入"7 颗珠子、6 色"**（必有一色两颗）：`holds` 改成"存在"语义、全排列去重、前端允许放第二颗同色；另加说法「两颗 X 挨着」→ `X~X`。README 补了「难度阶梯」表（各级实际用到哪些线索类型） |
| v0.3.0 | 2026-09-16 | **第三个游戏 `memory` 开工**（记忆：串珠记忆还原）。跟前面两个最大的不同：**程序只给规则卡、不存答案** —— 排列由出题人当场摆，判定也由出题人做，所以这是第一个"题库里没有答案、也没有 check 接口"的游戏。形状照旧同构：`core/colors.py` + `core/cards.py`（规则卡读写校验 + `list_levels`/`pick`/`load`）+ `api.py`（`/api/memory/...`，独立跑在 8020）+ `web/`（材料 / 题目 / 例子 + 倒计时，判定由人点）+ `puzzles/1/`~`puzzles/10/`（**100 张，L1~L10 每级 10 张**）+ 自己的 README（玩法、卡片格式、L1~L10 难度表）。**§7.3 说的"用第二个游戏检验抽象"到这一步才真跑通**：`list_levels` / `pick` / `load` 照样挂得上，一个"压根没有答案"的游戏也装得进这套骨架 |
| v0.3.1 | 2026-09-16 | **`group`（分类）不做，文件夹删掉**（`games/group/` 连同占位 README）。这套珠子只有颜色一种属性（6 色、同尺寸同形状），"把有共同点的放一起"能出的题只剩"把颜色分成几组"—— L1~L6 是同一件事，难度长不起来（占位 README 里举的"一样大的、都是圆的"这套珠子根本没有）。要真考分类，得先有第二条属性轴（比如不同尺寸的珠子）；想用现有珠子做，只能改成"分堆"（数量游戏，不算分类，得另起一个文件夹）。认知分类从六类变五类，`game.toml` 的 `category` 不再有 `group` |
| v0.3.2 | 2026-09-16 | **占位文件夹不再用了**：删掉 `games/plan/`、`games/sort/`、`games/space/`（连 README），新建 `games/pattern/`（找规律，占位 README：看出一串在按什么变、下一颗是什么）。分类改由 `game.toml` 的 `category` 记 —— 认知分类从五类变四类：规划 `plan`（`beadmatch`）、推理 `logic`、记忆 `memory`、找规律 `pattern`。其中"排序"那个位置由 `pattern` 接上，"空间"暂时不做 |
| v0.3.3 | 2026-09-16 | 加 `games/calc/`（计算，占位 README：用串珠数一数、比多少、做加减、分一分）。认知分类回到五类：规划 `plan`、推理 `logic`、记忆 `memory`、找规律 `pattern`、计算 `calc` |
| v0.3.4 | 2026-09-16 | `calc`（计算）**开了个头**：定下卡片格式（照 `logic` 写：注释区 + 元信息区 + 正文区；`participants` 带数量、正文就是规则句、`answer` 的类型跟着 `task` 走）、L1~L12 难度阶梯、1 张样例卡（`puzzles/1/1-01.txt` 数一数），以及**后端**（`core/colors.py` + `core/cards.py` + `api.py`，独立跑在 8030）。跟前面三个不同的是**它有 `POST /check`** —— 计算题的答案能判（数字 / 颜色 / equal 都收，中文数字认到二十），不用像记忆那样全靠人。前端还没做 |
| v0.3.5 | 2026-09-16 | `calc` **加前端**（`web/` 三件套，照记忆那版式）：材料 / 题目 / 孩子说 三块，下面一个输入框 +「对一下」；判定走 `/check`（对了「叮」、没对「咚」）。右上角「读一遍 / 看答案 / 换一题」，键盘 `回车` 对一下、`空格` 换一题。第四个游戏的形状齐了：`core/ + api.py + web/ + puzzles/ + README` —— 跟前面三个完全同构 |
| v0.3.6 | 2026-09-16 | 两件事：① `calc` 前端瘦身 —— 删掉「孩子说 / 对一下」那块，页面只剩 材料 / 题目 + 看答案（判定交回给人做，`/check` 留在后端备用）；② **`calc` 的 L1 出齐 10 张**，顺手把题目文件的命名统一成串珠 / logic 那个叫法 **`<日期>-<时间>-<4 位>`**（原来的 `1-01.txt` 也改了名）—— 规则写进 `games/README.md` 了，`memory` 那 100 张还没改 |
| v0.3.7 | 2026-09-16 | `calc` **L2~L12 也出齐**（每级 10 张，全库 120 张），难度照 L1~L12 那张表：一样多 → 比多少 → 加减 → 补数 → 拆两组 → 拆法找全 → 缺数 → 连加连减/三组 → 凑数 → 带条件的全部凑法。答案都是按材料算出来的；`task` 补了一个 `chain`（连续变化）。`calc` 现在是四个游戏里题目最全的一个 |
| v0.3.8 | 2026-09-16 | `calc` **8/9/10 换位**：原来 L7（拆成两组）直接跳 L8（拆法找全）太陡 —— 枚举"有几种分法"和算一个数不是同一种思维。改成 L8 缺数（符号）→ L9 连加连减/三组 → **L10 拆法找全**，于是 L10~L12 三档"数方法的个数"连在一起，L1~L9 全是单答案算术，全程只换一次挡。卡片只换位置和 `level=`，内容没动 |
| v0.4.0 | 2026-09-16 | **第 2 步做完：广场外壳立起来了**。四个游戏各加 `game.toml`；`api.py` 从"自带 FastAPI app"改成"导出 `router`"（单独跑端口照旧 8000 / 8010 / 8020 / 8030）；前端静态资源改**相对路径**、接口基址读 `window.API_BASE`（广场注入），一份代码两种跑法都能用；新增 `plaza/registry.py`（扫 `game.toml`）、`plaza/server.py`（挂路由 + 首页 + 静态托管）、`plaza/web/style.css`；`run.py` 改成起广场。URL 定案：广场 `/`、游戏 `/games/<id>/`、静态 `/games/<id>/static/...`、接口 `/api/games/<id>/...`。**验收**：首页列出四个游戏（串珠 300 / 推理 70 / 记忆 100 / 计算 120 题），四个游戏的页面、静态、接口在广场里全通，单独跑也全通 |
| v0.4.1 | 2026-09-17 | 广场给每个游戏页面**注入一条顶部导航**（`游戏广场 · 串珠 · 推理 · 记忆 · 计算`）：字小、淡色、用游戏自己的主题变量，当前那个是加粗文字不是链接 —— 需求是"互相跳转但别突兀"。做法放在广场的页面处理器里（`plaza/server.py` 的 `_nav_html`），**游戏侧零改动**，单独跑时不出现外链 |
| v0.4.2 | 2026-09-17 | 导航按反馈改了三处：① 位置从"标题下面"挪到**标题上面**（先导航后标题）；② **可点的带下划线、当前那个加粗不带下划线** —— 之前两者差别太小，看不出哪个是"我在这儿"、哪个能点；③ 样式挪进 `<head>`，不再先闪一下没样式。另外查出"点链接有声音"的真凶：不是点击本身，是**新打开的游戏页在拿第一道题时响了那声"嗒"** —— 四个游戏的首次加载都改成静音，主动按「换一题」照旧有声 |
| v0.4.3 | 2026-09-17 | **`beadsort`（竞技串珠）把规则换成了商业球排序那套**：一次只能放到空柱、或顶色相同的柱子上。连带解决了一个真问题 —— 竞技规则下**"合法乱走"打不乱局面**（只能同色，永远混不进两根柱子里），而**随机撒又基本无解**（小棋盘实测 0/20，跟当年旧规则 0/30 一个现象）；出题改成 **施工式反走**：把一条合法解**倒着演一遍**当题面，施工那一步不要求合法、只要求"拎得起"（搞定它顶上那颗拿走后露同色或拿空），于是混色出来了、**起点一定有解**、还不用求解器。校验加狠一条：解里每一步都必须在竞技规则下合法；旧题库（300 道借来的）清空，重新出了 **20 道 L1** |
| v0.4.4 | 2026-09-18 | 竞技串珠补上**采样器**（`run.py` 无改动，游戏自己的 `tools/sampler.py`）：纯随机反走 + 禁止走回老局面 + 停滞就停，每一步都是合法逆操作所以**局面一定有解**。实验结论：**走得更久 ≠ 出得更乱**（老做法硬走 374 步的乱度，跟走 21 步一样），离位上上限是**结构性的**（1 千 / 1 万 / 5 万道最高 23 → 27 → 27）。据此跑 10 万道重建题库：**100 道，1~5 级各 20 道**。另修一处页面排版：竞技串珠的棋盘被底下那行"规则"文案撑宽后珠子贴着左边排，改 `.board { justify-content: center }` 后回到页面中线 |
| v0.4.5 | 2026-09-18 | 竞技串珠的**出题改成"方法制"**：唯一入口 `tools/make.py`（`--method` + `--n`=尝试次数 + `--out`），`core/methods/` 里一个方法一个模块 —— **加第三种方法只要加一个文件**。方法一 `walk` = 施工式反走（沿用）；**方法二 `kociemba` = 随机摆 + 求解器筛**：把 2026-09-14 删掉的 Kociemba 移植从 git 历史里捞回来（字节没变，连它那份暴力 BFS 对拍测试一起），随机摆一个局面让它解、**解出来才留**。实测真配置 1 万次尝试 50 秒出 5 道（0.05%），5 道都用规则引擎验过是真解、解长 122~146 步。原因见 [requirement.md](requirement.md) §8.2 |
